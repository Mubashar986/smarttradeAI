# SmartTrade C2 Rust Workspace

This Rust workspace powers the active SmartTrade C2 orchestration service.

It contains:

- provider clients
- the conversation/runtime loop
- the HTTP and SSE server
- built-in tool execution
- plugin loading used by SmartTrade

Historical upstream Rust-facing docs were preserved under `../docs/upstream/rust/`.
